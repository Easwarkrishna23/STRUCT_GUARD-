# Phase 4/5 Attack Results — Invalidated Legacy Output

The legacy Phase 4/5 table has been invalidated because it contained attacks and defenses that do not satisfy the current mandatory thesis gates.

Current gates:

| Gate | Requirement |
| --- | --- |
| Attack degradation | `baseline_acc - attacked_acc >= 0.50 * baseline_acc` |
| Defense recovery | `defended_acc >= baseline_acc` |
| Injected-edge pruning | `injected_edge_prune_rate >= 90%` |

The accepted result table must be regenerated by:

```bash
python run_full_pipeline.py
```

The pipeline now refuses to accept stale cache files and raises an error when any row violates the attack, recovery, or pruning gates.
